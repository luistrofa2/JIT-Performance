### Scripts

- `install_dependencies.sh`: Installs needed dependencies to be able to create graphics in `project.ipynb`.

---

### NOTE

If a virtual environment has not been created yet, that is used in the other parts of the project, then additional steps need to taken. 
 
1. In the main folder of the project (JIT-Performance) execute:
```bash
python3 -m venv venv
```
