### Scripts

- `copy_Makefile.sh`: Copies Makefile file to all subfolders. Each Language contains a general Makefile that can be edited, and then copied to each benchmark with this script.
- `copy_phpini.sh`: Copies php.ini file to all subfolders. PHP contains a general php.ini file that can be edited, and then copied to each benchmark with this script.
